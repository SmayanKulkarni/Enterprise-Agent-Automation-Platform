# Contain agentic reasoning within a durable workflow shell

Durable orchestration will own timers, retries, approvals, compensation, and state transitions, while bounded agentic stages may investigate, plan, delegate, use capabilities, and revise their approach. Each stage has an explicit goal, budget, exit criteria, and allowed transitions so the system remains recoverable without reducing agents to isolated LLM calls.
