# Build a multi-tenant platform with two reference deployments

The product will isolate organizations and demonstrate reuse through two reference deployments: enterprise customer onboarding and vendor-risk/access review. Sharing the platform while separating each tenant's identities, policies, connectors, memory, and telemetry is intentional evidence of platform engineering rather than two unrelated demos.
